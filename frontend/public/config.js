// Configuración en tiempo de ejecución
// Este archivo se puede modificar sin recompilar el frontend
window.APP_CONFIG = {
  BACKEND_URL: window.APP_CONFIG?.BACKEND_URL || 'http://localhost:8081'
};

