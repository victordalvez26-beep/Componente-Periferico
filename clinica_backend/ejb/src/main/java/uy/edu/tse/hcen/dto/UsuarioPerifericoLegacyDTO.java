package uy.edu.tse.hcen.dto;

/**
 * Legacy compatibility class renamed to avoid duplicate-class collisions during refactor.
 * If code still needs `UsuarioPerifericoDTO`, prefer using `DTUsuarioPeriferico` or add a
 * dedicated wrapper. This class intentionally uses a different name to avoid conflicts.
 */
public class UsuarioPerifericoLegacyDTO extends DTUsuarioPeriferico {
    public UsuarioPerifericoLegacyDTO() { super(); }
}
